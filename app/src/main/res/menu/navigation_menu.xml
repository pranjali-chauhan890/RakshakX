<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <!-- Profile -->
        <item
            android:id="@+id/nav_profile"
            android:icon="@android:drawable/ic_menu_myplaces"
            android:title="Profile" />

        <!-- Contact Emergency Services -->
        <item
            android:id="@+id/nav_emergency"
            android:icon="@android:drawable/ic_menu_call"
            android:title="Contact Emergency Services" />

        <!-- SOS History -->
        <item
            android:id="@+id/nav_sos_history"
            android:icon="@android:drawable/ic_menu_recent_history"
            android:title="SOS History" />

        <!-- Settings (Alag Option) -->
        <item
            android:id="@+id/nav_settings"
            android:icon="@android:drawable/ic_menu_preferences"
            android:title="Settings" />

        <!-- Top Questions (Alag Option) -->
        <item
            android:id="@+id/nav_top_questions"
            android:icon="@android:drawable/ic_menu_help"
            android:title="Top Questions" />

        <!-- About Us (Andar FAQ honge) -->
        <item
            android:id="@+id/nav_about_us"
            android:icon="@android:drawable/ic_menu_info_details"
            android:title="About Us" />

        <!-- Logout -->
        <item
            android:id="@+id/nav_logout"
            android:icon="@android:drawable/ic_menu_close_clear_cancel"
            android:title="Logout" />

    </group>

</menu>